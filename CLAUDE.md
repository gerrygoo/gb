# CLAUDE.md

## Project

Browser-based GIF creation tool. See `docs/scoping.md` for design decisions
and `docs/plan.md` for the phased implementation plan.

## Workflow

Solo developer repo. Push directly to `main`. Do not create pull requests
unless explicitly asked.

## Tech stack

Vite + TypeScript + Svelte, WebCodecs, WebGPU, mp4box.js.
