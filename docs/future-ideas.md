# Future Ideas (post-MVP, not planned in detail)

Unscoped items pulled from `docs/scoping.md`'s feature list that aren't
part of the active plan (`docs/plan.md`). Move an item into a real phase
there when it's ready to be worked on.

- WebM demuxer
- Multi-segment compile (t3, t5)
- Frame deletion (t4), reverse (t6), boomerang (t7)
- Frame decimation (t9)
- Crop (n1), text overlay (n2), filters (n3)
- Per-frame byte breakdown (s2), "what if" deltas (s3)
- Clipboard copy (o2), multi-size export (o3)
- Move WebGPU to worker via OffscreenCanvas
